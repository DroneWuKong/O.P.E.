# O.P.E. UI

Minimal mission-first interface.

Default view:

```text
What needs to get done?
```

The interface hides routes, providers, memory, tool queues, costs, and diagnostics until the operator opens Advanced.
